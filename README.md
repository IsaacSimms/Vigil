# Vigil
A domain-agnostic observability engine in C#/.NET: ingest event streams, normalize to a common shape, query history, detect deviation, and alert on rules. Infrastructure monitoring is the first adapter set.
